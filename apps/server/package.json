{
  "name": "@aether/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit",
    "lint": "prettier --check src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aether/application": "workspace:*",
    "@aether/auth": "workspace:*",
    "@aether/contracts": "workspace:*",
    "@aether/database": "workspace:*",
    "@fastify/cookie": "^11.0.0",
    "fastify": "^5.0.0",
    "pg": "^8.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@aether/testkit": "workspace:*"
  }
}
