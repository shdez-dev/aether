/** Configuración compartida que no contiene secretos. */
export const configuration = Object.freeze({});

/** Public product identity; never put credentials or runtime secrets here. */
export const publicProduct = Object.freeze({
  name: "Aether",
  demoEmail: "juanhernandezr0075@gmail.com",
});
